---
- name: Push that repo!
  local_action: "command git --git-dir=\"{{ playbook_dir }}/{{ gitpush_local_repo_path }}\" push \"{{ ansible_user }}@{{ ansible_host }}:{{ gitpush_repo_path }}\" HEAD:master"
